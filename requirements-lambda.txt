# Runtime-only dependency set for the deployed Lambda image. Trimmed from
# requirements.txt (which stays the source of truth for local dev/tests):
#   - torch is installed separately in the Dockerfile from the CPU-only wheel
#     index; torchvision/torchaudio are dropped entirely (grep confirms
#     nothing in this repo imports either).
#   - TA-Lib is dropped: app/data/preprocessor.py already falls back to a
#     pure-Python indicator implementation when the talib C library isn't
#     present (see README.md, "TA-Lib is optional"), and TA-Lib 0.4.27 has no
#     prebuilt manylinux wheel, so installing it would require compiling the
#     upstream C library in the image for no runtime benefit.
#   - matplotlib/seaborn/plotly/tqdm are dev/notebook visualization tooling,
#     never imported by serve_api.py's import chain.
#   - pytest*/black/flake8/mypy are dev/test tooling, not runtime deps.
# Every version pin below is copied verbatim from requirements.txt.

numpy==1.24.3
pandas==2.0.3
scipy==1.11.1

transformers==4.30.2
tokenizers==0.13.3
sentencepiece==0.1.99

statsmodels==0.14.6
arch==6.2.0
hmmlearn==0.3.3

yfinance==0.2.28
requests==2.31.0
urllib3==2.0.4

scikit-learn==1.3.0

fastapi==0.100.0
uvicorn[standard]==0.23.1
pydantic==2.0.3

python-dotenv==1.0.0
pyyaml==6.0
loguru==0.7.0

# Lambda-specific
mangum==0.17.0
