# ============================================================
# NEURO-ECONOMETRIC MARKET ALPHA ENGINE - DEPENDENCIES
# Python 3.11+
# ============================================================

# Core Scientific Computing
numpy==1.24.3
pandas==2.0.3
scipy==1.11.1

# Deep Learning Framework
torch==2.0.1
torchvision==0.15.2
torchaudio==2.0.2

# NLP & Transformers
transformers==4.30.2
tokenizers==0.13.3
sentencepiece==0.1.99

# Econometrics & Time Series
# NOTE: 0.14.0-0.14.2 wheels have a Cython/numpy-2.x ABI incompatibility that
# breaks statsmodels.tsa.statespace (used by ARIMA/SARIMAX) -- "numpy.dtype
# size changed, may indicate binary incompatibility". 0.14.6 fixes this.
statsmodels==0.14.6
arch==6.2.0
hmmlearn==0.3.3

# Technical Indicators
TA-Lib==0.4.27

# Data Acquisition
yfinance==0.2.28
requests==2.31.0
urllib3==2.0.4

# Machine Learning Utilities
scikit-learn==1.3.0

# API Framework
fastapi==0.100.0
uvicorn[standard]==0.23.1
pydantic==2.0.3
mangum==0.17.0  # ASGI adapter for the Lambda deployment target (Dockerfile.lambda); also
                # imported unconditionally at the bottom of serve_api.py, so it must be
                # installed for local/Docker-Compose runs of the same file too.

# Data Visualization (Optional)
matplotlib==3.7.2
seaborn==0.12.2
plotly==5.15.0

# Progress Bars
tqdm==4.65.0

# Testing
pytest==7.4.0
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Code Quality
black==23.7.0
flake8==6.0.0
mypy==1.4.1

# Utilities
python-dotenv==1.0.0
pyyaml==6.0

# Logging
loguru==0.7.0

# ============================================================
# INSTALLATION NOTES
# ============================================================
# 
# 1. TA-Lib requires binary installation:
#    - Windows: Download from https://www.lfd.uci.edu/~gohlke/pythonlibs/#ta-lib
#    - Linux: sudo apt-get install ta-lib
#    - macOS: brew install ta-lib
#
# 2. For GPU support (CUDA 11.8):
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# 3. For CPU-only:
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# 4. NewsAPI Key:
#    Register at https://newsapi.org/ and update Config.NEWS_API_KEY
#
# ============================================================
